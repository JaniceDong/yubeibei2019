#include <stdio.h>
int main (void) {
  int radius;
  printf("please input a value of radius:\n");
  scanf ("%d\n", &radius);
  
  double cc;
  double PI = 3.1415926;
  cc = PI * radius *radius;
  printf ("%lf\n", cc);
  printf ("it is only for test git!");
  printf ("it is only for test git!");
  printf ("it is only for test git!");
  
  return 0;
}
